// Implicit Type Inference
// TypeScript automatically infers the type based on the assigned value.
let a = "Vineeth";

// The variable 'a' is inferred as a string.
// a = 10; // Error: Type 'number' is not assignable to type 'string'.

// Explicit Type Inference
// We explicitly specify the type of the variable.
let b: string = "Vinoth";

// The variable 'b' is explicitly declared as a string.

// JavaScript is dynamically typed.
// The variable type can change during runtime.

// TypeScript is statically typed.
// The variable type is checked during development/compile time.

// Implicit Type Inference
// TypeScript automatically determines the variable type.